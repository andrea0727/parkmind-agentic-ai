# notebooks/

Exploratory work only — TouringPlans.com historical data exploration,
persona-generation experiments, ad-hoc groundedness spot-checks. Nothing
here is imported by the application; if an exploration turns into
something the app needs, move that logic into `src/parkmind/services/`.
