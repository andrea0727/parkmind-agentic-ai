# Requires Python 3.12.9 — see .python-version and README "Getting started".
# Pinned for stability across the team's machines; other 3.12.x may work
# but 3.12.9 is what we've actually tested against.

# Runtime
pydantic>=2.6
langgraph>=0.2
langchain-anthropic>=0.2
streamlit>=1.38
httpx>=0.27
psycopg[binary]>=3.2
python-dotenv>=1.0

# Dev / testing
pytest>=8.0
pytest-cov>=5.0
ruff>=0.6

# Observability
langfuse>=2.0